arr = [12, 35, 1, 10, 34, 1]
largest = second_largest = -1

for num in arr:
    if num > largest:
        second_largest = largest
        largest = num
    elif num > second_largest and num != largest:
        second_largest = num

if second_largest == -1:
    print("Second largest element doesn't exist: -1")
else:
    print("Second largest element:", second_largest)
