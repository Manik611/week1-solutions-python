arr = [10, 20, 10, 5, 20]
visited = [0] * len(arr)

for i in range(len(arr)):
    if visited[i]:
        continue
    count = 1
    for j in range(i + 1, len(arr)):
        if arr[i] == arr[j]:
            visited[j] = 1
            count += 1
    print(f"[{arr[i]}, {count}]")
